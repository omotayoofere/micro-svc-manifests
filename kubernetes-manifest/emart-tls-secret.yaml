apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: emart-tls-secret
  namespace: emart-app
spec:
  refreshInterval: "30m"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: emart-tls-secret
    creationPolicy: Owner
    template:
      type: kubernetes.io/tls
  data:
  - secretKey: tls.crt
    remoteRef:
      key: secret/data/k8s/tls/emart-ingress-cert
      property: tls.crt
  - secretKey: tls.key
    remoteRef:
      key: secret/data/k8s/tls/emart-ingress-cert
      property: tls.key
